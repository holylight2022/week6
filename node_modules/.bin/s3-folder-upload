#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../s3-folder-upload/bin/s3-folder-upload.js" "$@"
  ret=$?
else 
  node  "$basedir/../s3-folder-upload/bin/s3-folder-upload.js" "$@"
  ret=$?
fi
exit $ret
